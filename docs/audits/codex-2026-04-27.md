# Codex Audit — 2026-04-27

**Commit**: 95afa7e
**Tokens**: 417002
**Verdict**: ❌ 不建議上線（pre-launch fail）— P0 x2, P1 多項

---

## 1. 併發 / 資源洩漏

- **P1** `_queue` 無鎖，兩 executor 拿到同檔重複執行 — `scripts/auto-loop.sh:19,40`
  Fix: `flock` / atomic `mv _queue/x.md → _queue/.running/x.md`
- **P1** `/auto` 無 `INT/TERM` trap，Ctrl+C 時 queue 不一致 — `.claude/commands/auto.md:14-26`
  Fix: trap 強制補 `Status: ⚠️ blocked` + archive
- **P2** notify-blocked 每次背景起 `afplay` 無節流 — `scripts/notify-blocked.sh:5`
  Fix: cooldown + singleton lock

## 2. 失敗模式

- **P1** shared sync 失敗吞 stderr，只剩 generic WARN — `bootstrap.sh:280, 611`
  Fix: 不 `>/dev/null 2>&1`，保留 stderr 摘要
- **P1** `/inbox` 與 `/auto` 可並跑無互斥，commit/push 互撞 — `inbox.md:5-12, auto.md:11-26`
  Fix: executor 開始前 `.git/tandem-executor.lock`
- **P2** shared-init owner 硬編碼 — `scripts/shared-init.sh:7,21-25`
  Fix: 用 `gh api user` 決定 owner

## 3. User UX

- **P1** notify 完全可能 silent fail（osascript/say 全吞）— `scripts/auto-loop.sh:56-60`
  Fix: 失敗時 `echo "[notify] failed"` 到 stderr
- **P2** `/auto` 文件對「queue 空通知」自相矛盾 — `auto.md:12 vs :37`
  Fix: 統一規格

## 4. 長時間穩定

- **P1** shared 用 `git add -A`，可能推非預期檔 — `memory.sh:66, lessons.sh:31`
  Fix: path-scoped add（只 `memory/*.md`, `lessons/*.md`）
- **P2** `_archive/` 無 retention — `archive-prompts.sh:90-131`
  Fix: 月度 squash/tag
- **P2** statusline 1Hz 全檔掃描 — `scripts/statusline.sh:11,23,38`
  Fix: mtime cache

## 5. 死碼 / 不同步

- **P1** lessons staging 路徑不一致，pipeline 斷 — `archive-prompts.sh:11, statusline.sh:36, lessons.sh:12`
  Fix: 單一常數路徑（建議 `~/.claude-work/shared/lessons-staging.md`）
- **P1** `Result` 格式 `Status:` vs `**Status**` 不一致，detector 抓不到 — `inbox.md:18, auto.md:18, archive-prompts.sh:40`
  Fix: detector 雙支援
- **P2** `/brief` 依賴 `project_current_handoff` 但無穩定產生器 — `brief.md:9`
  Fix: `/inbox` 收尾固定寫 handoff

## 6. SLO

- **P1** statusline 延遲 OK（40-50ms 量測），但 line 39 噴 `integer expression expected` stderr — `scripts/statusline.sh:38-39`
  Fix: `n_lessons=${n_lessons:-0}` 不要 `|| echo 0`
- **P1** `/auto fail-stop` 是文字規範，line 38 又寫「failed/skipped 後繼續」自打 — `auto.md:25-26,38,44`
  Fix: fail-stop 下沉到 script state machine

## 7. Prompt Injection / 安全（最危險）

- **P0** `/inbox`/`/auto` 規範 executor「完全照 prompt 執行不二次推理」= `_inbox/_queue` 變 root-of-trust — `inbox.md:10, auto.md:13`
  Fix: preflight policy gate（危險命令白名單 / 需人工確認）
- **P0** shared remote 被入侵時，可用 symlink 夾帶本機敏感檔（e.g. `memory/secret.md → ~/.ssh/id_rsa`）— `memory.sh:152-170,46`
  Fix: 拒絕來源 symlink、verify realpath、簽章/受信 branch
- **P1** `auto-loop archive` 未限制 path 必須在 `_queue`，可搬任意檔 — `scripts/auto-loop.sh:27-30`
  Fix: canonicalize + force prefix 在 `docs/prompts/_queue/`

---

## Worst-case

shared remote 被污染 + `_inbox/_queue` 注入同時發生 → executor 依規範無條件執行惡意指令 + 跨專案污染 memory 同步到所有 repo + 通知靜默失敗 → 資料外洩或破壞後才被發現。

---

## 動作建議

依 CODEX_AUDIT.md verdict map：2 P0 + 7 P1 → **拆批 fix prompts**，每批 ≤3 個 P0/P1。

**建議批次**：
- **批 1（安全 P0）**：prompt injection gate + shared symlink 拒絕 + auto-loop archive path validation（3 fixes）
- **批 2（並發 P1）**：queue file lock + Ctrl+C trap + /inbox+/auto executor lock（3 fixes）
- **批 3（可靠性 P1）**：silent notify fix + bootstrap stderr 保留 + path-scoped git add（3 fixes）
- **批 4（一致性 P1）**：lessons staging path 統一 + Result 格式 detector + /auto fail-stop state machine（3 fixes）
- **批 5（P2 收尾）**：retention / cooldown / mtime cache / handoff generator（雜項，可延後）
