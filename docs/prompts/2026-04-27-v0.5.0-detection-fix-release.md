# v0.5.0 release: detection regex fix + cut + GH release

## Goal

cut **v0.5.0**（Phase B + Phase C）+ 修 Phase C detection regex（current 太寬產生 2 條 false positives）+ 清 staging false positives + tag + GH release + README/CHANGELOG。

## Execution profile

- model: sonnet
- effort: medium（detection regex 修正要小心、3 commits、release ops）
- 3 commits（detection fix / README / CHANGELOG）+ archive = 4
- 加：staging cleanup + git tag v0.5.0 + push + gh release create

## User-authorised remote ops（**承襲 v0.4.2 慣例 + 本輪明文**）

`gh release create` + `git tag` 已 user 拍板（v0.5.0 release cut + B option，detection fix 同輪做）→ executor 直接跑。

## Detection regex 為什麼要修

Phase C archive 跑完 staging 出現 2 個 raw entries，**都是 false positives**：

```
[1] 2026-04-27-phase-b-...                    [2] 2026-04-27-v0.4.2-release-...
    signals:                                       signals:
    - "**Status**: ✅ shipped | ❌ blocked"         - "**Status**: ✅ shipped | ❌ blocked"
    - "FAIL: not in Tandem"                        - "FAIL: not in Tandem"
```

兩個檔的 actual status 都是 `✅ shipped`，FAIL 訊號是 inbox prompt 裡 bash code block 的 echo 字串（`echo "FAIL: not in Tandem"`，是 pre-flight 失敗訊息，不是 verification fail）。

**Bug 1**：current regex `^\*\*Status\*\*.*❌` 匹配 Status template line `**Status**: ✅ shipped | ❌ blocked`（佔位符模板）。需區分「結尾 ❌」vs「字串中含 ❌」。

**Bug 2**：current FAIL detection 掃整個檔，包括 inbox prompt 裡的 bash code blocks。應該只掃 Result block 區段，且 skip ``` 裡的內容。

**Bug 3 (順手修)**：current Blocker detection regex `grep -viE 'none|^\*\*Blockers\*\*: *$'` 漏 filter `<description>` 模板佔位符。

## Files to edit

1. `scripts/archive-prompts.sh` — detection 邏輯改寫
2. `~/.claude-work/_shared/lessons-staging.md` — 清 2 條 false positive
3. `README.md` — Roadmap 標 Phase B/C done + v0.5.0
4. `CHANGELOG.md` — [Unreleased] → [0.5.0]

外部：`git tag v0.5.0` + push + `gh release create`

## Step-by-step

### Step 1: Pre-flight

```bash
pwd | grep -q "/Tandem$" || { echo "FAIL: not in Tandem"; exit 1; }
[ -z "$(git status --porcelain)" ] || { echo "FAIL: working tree dirty"; git status --short; exit 1; }
gh auth status >/dev/null 2>&1 || { echo "FAIL: gh not authed"; exit 1; }
git tag -l | grep -q "^v0.5.0$" && { echo "FAIL: tag v0.5.0 already exists"; exit 1; } || true

# verify the 2 false positives still in staging（如果 user 已手動清掉就 skip step 3）
n_staging_before=$(grep -c '^<!-- BEGIN entry' "$HOME/.claude-work/_shared/lessons-staging.md" 2>/dev/null || echo 0)
echo "staging entries before fix: $n_staging_before"

echo "PASS: pre-flight"
```

### Step 2: 修 scripts/archive-prompts.sh detection

讀現檔找到 `detect_and_stage_lesson()` 函式。整段重寫（用 Edit tool 替換）。

**新版**：

```bash
detect_and_stage_lesson() {
    local archive_file="$1"
    local id
    id=$(basename "$archive_file" .md)

    [ "${DRY_RUN:-0}" = 1 ] && return 0
    [ -d "$(dirname "$STAGING")" ] || return 0

    if [ -f "$STAGING" ] && grep -q "^<!-- BEGIN entry id=$id " "$STAGING"; then
        return 0
    fi

    # Extract ## Result block content, skipping fenced code blocks
    local result_content
    result_content=$(awk '
        /^## Result$/ {in_result=1; next}
        in_result && /^## / {in_result=0}
        in_result && /^```/ {in_code = !in_code; next}
        in_result && !in_code {print}
    ' "$archive_file")

    [ -z "$result_content" ] && return 0

    # Bug 1 fix: Status is "blocked" only when ❌ present and ✅ NOT present on same line
    local status_line has_blocked=""
    status_line=$(echo "$result_content" | grep -m1 -E '^\*\*Status\*\*' || true)
    if [ -n "$status_line" ] && echo "$status_line" | grep -q "❌" && ! echo "$status_line" | grep -q "✅"; then
        has_blocked="$status_line"
    fi

    # Bug 3 fix: Blockers must be non-template (none / <description> / empty all skip)
    local has_blockers=""
    local blocker_line
    blocker_line=$(echo "$result_content" | grep -m1 -E '^\*\*Blockers\*\*:' || true)
    if [ -n "$blocker_line" ] && ! echo "$blocker_line" | grep -qiE 'none|<description>|^\*\*Blockers\*\*: *$'; then
        has_blockers="$blocker_line"
    fi

    # Bug 2 fix: FAIL only counts inside Result block, outside code fences, AND not on a "PASS / FAIL" template line
    local has_fail=""
    local fail_line
    fail_line=$(echo "$result_content" | grep -E '\bFAIL\b' | grep -v 'PASS' | head -1 || true)
    [ -n "$fail_line" ] && has_fail="$fail_line"

    # Keywords (also Result-block-scoped now)
    local has_keyword=""
    has_keyword=$(echo "$result_content" | grep -m1 -iE 'next time|should (have|do|run|abort)|lesson learned' || true)

    if [ -z "$has_blocked" ] && [ -z "$has_blockers" ] && [ -z "$has_fail" ] && [ -z "$has_keyword" ]; then
        return 0
    fi

    mkdir -p "$(dirname "$STAGING")"
    local ts
    ts=$(date -u +"%Y-%m-%dT%H:%M:%SZ")

    {
        echo "<!-- BEGIN entry id=$id state=raw timestamp=$ts -->"
        echo "- archive: $archive_file"
        [ -n "$has_blocked" ] && echo "- status: ❌ blocked"
        echo "- signals:"
        [ -n "$has_blocked" ] && echo "  - \"$has_blocked\""
        [ -n "$has_blockers" ] && echo "  - \"$has_blockers\""
        [ -n "$has_fail" ] && echo "  - \"$has_fail\""
        [ -n "$has_keyword" ] && echo "  - \"$has_keyword\""
        echo "- excerpt: |"
        awk '/^## Result$/,0' "$archive_file" | head -25 | sed 's/^/    /'
        echo "<!-- END entry -->"
        echo ""
    } >> "$STAGING"

    echo "[archive] lesson signal detected → staged: $id"
}
```

### Step 3: 清 2 條 false positive from staging

```bash
STAGING="$HOME/.claude-work/_shared/lessons-staging.md"

if [ -f "$STAGING" ]; then
    # 兩條都是 raw + false positive，簡單清整檔（staging 應該空）
    : > "$STAGING"
    echo "PASS: staging cleared (2 false positives removed)"
else
    echo "WARN: staging file missing — skipping cleanup"
fi
```

(也可以選擇性移除 entry，但兩條都該砍 → 簡單清空效率更高。)

### Step 4: 用 fixed regex 重跑 detection 驗 false positive 不再

跑一次「mock detection」：對 Phase B + v0.4.2 兩個 archive 餵新的 detect function，期望**不**觸發。

```bash
# Source archive-prompts.sh 取得 detect_and_stage_lesson function
# 但 archive-prompts.sh 是 main script 不是 lib，要用 subshell + 變數注入

# 簡化版 verify：直接 grep 檢查 fixed regex 邏輯
for f in docs/prompts/_archive/2026-04/2026-04-27-phase-b-model-guide-execution-profile.md \
         docs/prompts/_archive/2026-04/2026-04-27-v0.4.2-release-readme-polish-dogfood-p3.md; do
    [ -f "$f" ] || { echo "skip (missing): $f"; continue; }

    # Status line：has ❌ AND not ✅
    status_line=$(awk '/^## Result$/,0' "$f" | grep -m1 -E '^\*\*Status\*\*' || true)
    if [ -n "$status_line" ] && echo "$status_line" | grep -q "❌" && ! echo "$status_line" | grep -q "✅"; then
        echo "FAIL: $f still triggers (has_blocked)"
        exit 1
    fi

    # FAIL outside code fences in Result section
    result_content=$(awk '
        /^## Result$/ {in_result=1; next}
        in_result && /^## / {in_result=0}
        in_result && /^```/ {in_code = !in_code; next}
        in_result && !in_code {print}
    ' "$f")
    if echo "$result_content" | grep -E '\bFAIL\b' | grep -v 'PASS' | head -1 | grep -q .; then
        echo "FAIL: $f still triggers (has_fail in Result block)"
        exit 1
    fi

    echo "PASS: $f no longer triggers"
done
```

要兩個都 PASS。

### Step 5: README.md — Roadmap 更新

把 Roadmap 段裡：
```markdown
- [ ] **Phase B** — model + effort recommendation system (was 4e)
- [ ] **Phase C** — learning loop (`/lessons` slash)
```

改成：
```markdown
- [x] **Phase B** — model + effort selection guide (`docs/MODEL_GUIDE.md` + `## Execution profile` convention)
- [x] **Phase C** — lessons loop (auto-stage on archive → `lessons.sh extract`/`review` → promote to shared memory; detection regex precision fixed in v0.5.0)
- [x] **v0.5.0 release** — Phase B + Phase C + lesson detection precision fix
```

「~~Phase 3~~ deferred」那行保持在最後。

### Step 6: CHANGELOG.md — [Unreleased] → [0.5.0] cut

當前 [Unreleased] 是 `(empty — next round adds entries)`。

把該段內容**整段替換**成：

```markdown
## [Unreleased]

(empty — next round adds entries)

## [0.5.0] - 2026-04-28

### Added

- **Phase C: lessons loop — auto-extracted feedback memories from inbox archives.** New `scripts/lessons.sh` (subcommands: count/list/extract/review) plus detection logic in `scripts/archive-prompts.sh`. Whenever an inbox round finishes with `Status: ❌ blocked`, non-empty `Blockers:`, a `FAIL` line in verification, or "next time/should/lesson learned" keyword hits, the archive flow appends a `state=raw` entry to `~/.claude-work/_shared/lessons-staging.md`. `lessons.sh extract` invokes `claude -p` headless to refine raw signals into structured candidates (frontmatter + Why/How-to-apply body); `lessons.sh review` walks each candidate interactively (default action = promote-to-shared). Promoted lessons land in `~/.claude-work/_shared/memory/<slug>.md` with an index row appended. See [docs/LESSONS.md](docs/LESSONS.md). This is the招牌 feature delivering the cross-project self-improving promise: lessons captured in project A become available to project B after promotion.
- **Phase B: model + effort selection guide.** New `docs/MODEL_GUIDE.md` distills empirical recommendations from 20+ archived inbox rounds — Claude as primary worked example with a principle-extension section for other planner/executor model pairs (reasoning-strong → planner, execution-strong → executor). New convention: every inbox prompt declares a `## Execution profile` block (model + effort + commits) near the top. Soft convention only (no lint), reminded via comment block in `templates/prompts/_inbox.md` and documented in `docs/REFERENCE.md`.
- **Statusline lessons indicator + briefing block.** Statusline appends `· 🎓 N` when staging is non-empty. SessionStart briefing prints a `=== lessons pending ===` block with raw/candidate breakdown.

### Fixed

- **Lesson detection regex precision.** Phase C's first-iteration detection produced 2 false positives on initial dogfood — Status template line `**Status**: ✅ shipped | ❌ blocked` and bash code blocks containing `echo "FAIL: ..."` both matched the heuristic. Fixed by (a) only matching Status as blocked when ❌ is present *and* ✅ is absent from the line, (b) restricting FAIL/keyword scans to the `## Result` block content with code fences stripped, (c) filtering Blockers template placeholders (`<description>`). Cleared the 2 false-positive raw entries from staging in the same round.

### Changed

- **`templates/CLAUDE.md` Workflow section is model-agnostic** (Phase A leftover caught in Phase B). Workflow describes the role (reasoning-strong planner / execution-strong executor) with Claude Code as my-setup example, links `docs/MODEL_GUIDE.md` for selection guidance.
- **`scripts/test-bootstrap.sh` now asserts 36/36** (was 32/32) — added 4 new assertions for `scripts/lessons.sh` (existence, executable bit, `count` subcommand runs, `help` subcommand runs).
```

### Step 7: 中段 verification

```bash
# 7a. archive-prompts.sh detection function 改了
grep -q "in_result && /\^\`\`\`/ {in_code = !in_code" scripts/archive-prompts.sh && echo "PASS: detection skips code fences"
grep -q '! echo "\$status_line" | grep -q "✅"' scripts/archive-prompts.sh && echo "PASS: detection requires no ✅"

# 7b. staging cleared
n_staging=$(grep -c '^<!-- BEGIN entry' "$HOME/.claude-work/_shared/lessons-staging.md" 2>/dev/null || echo 0)
[ "$n_staging" = "0" ] && echo "PASS: staging cleared" || { echo "FAIL: staging still has $n_staging entries"; exit 1; }

# 7c. lessons.sh count reflects
bash scripts/lessons.sh count 2>&1 | head -1

# 7d. CHANGELOG cut
grep -q "^## \[0.5.0\] - 2026-04-28" CHANGELOG.md && echo "PASS: [0.5.0] heading"
sed -n '/^## \[0.5.0\]/,/^## \[/p' CHANGELOG.md | grep -q "Phase C: lessons loop" && echo "PASS: Phase C entry"
sed -n '/^## \[0.5.0\]/,/^## \[/p' CHANGELOG.md | grep -q "Phase B: model + effort" && echo "PASS: Phase B entry"
sed -n '/^## \[0.5.0\]/,/^## \[/p' CHANGELOG.md | grep -q "Lesson detection regex precision" && echo "PASS: Fixed entry"

# 7e. README Roadmap
grep -q "\[x\] \*\*Phase B\*\*" README.md && echo "PASS: Phase B done"
grep -q "\[x\] \*\*Phase C\*\*" README.md && echo "PASS: Phase C done"
grep -q "\[x\] \*\*v0.5.0 release\*\*" README.md && echo "PASS: v0.5.0 row"

# 7f. test-bootstrap
bash scripts/test-bootstrap.sh && echo "PASS: test-bootstrap 36/36" || { echo "FAIL"; exit 1; }

# 7g. bash syntax
bash -n scripts/archive-prompts.sh && echo "PASS: archive-prompts syntax"
```

要全 PASS 才繼續。

### Step 8: Commits（atomic）

```bash
# Commit 1: detection regex fix
git add scripts/archive-prompts.sh
git commit -m "fix: lesson detection regex precision (Phase C self-dogfood)

Phase C's first-iteration detection produced 2 false positives on initial dogfood:
- '**Status**: ✅ shipped | ❌ blocked' template line matched ❌ heuristic
- bash code blocks containing 'echo \"FAIL: not in Tandem\"' triggered FAIL detection

Fixes:
- Status detected as blocked ONLY when line has ❌ AND no ✅
- FAIL/keyword scans now restricted to ## Result block content
- Code fences (\`\`\`) stripped before scan
- Blockers template placeholder '<description>' added to filter

Cleared the 2 false-positive raw entries from ~/.claude-work/_shared/lessons-staging.md
in the same round (cleanup is user state, not in this commit)."

# Commit 2: README Roadmap
git add README.md
git commit -m "docs: README Roadmap marks Phase B + C + v0.5.0 done

Phase B (MODEL_GUIDE.md + Execution profile convention) shipped 2c58b6b..24f6812.
Phase C (lessons loop) shipped 9335889..62e9374.
v0.5.0 release tagged in this round (with detection regex precision fix)."

# Commit 3: CHANGELOG cut
git add CHANGELOG.md
git commit -m "chore: cut CHANGELOG [Unreleased] → [0.5.0] - 2026-04-28

Includes:
- Phase B: docs/MODEL_GUIDE.md heuristic table + Execution profile soft convention + templates/CLAUDE.md model-agnostic
- Phase C: scripts/lessons.sh + archive-prompts.sh detection + statusline/briefing surfacing + docs/LESSONS.md + 4 new test-bootstrap assertions (32→36)
- Fixed: lesson detection regex precision (Phase C self-dogfood caught 2 false positives — root-caused and fixed in same release)"
```

### Step 9: Tag + Push + Release

```bash
git push origin main

git tag -a v0.5.0 -m "v0.5.0 — Cross-project self-improving lessons loop + model selection guide"
git push origin v0.5.0

gh release create v0.5.0 --title "v0.5.0 — Self-improving lessons loop" --notes "$(cat <<'EOF'
## Highlights

This release delivers the **cross-project self-improving** promise that was the招牌 wedge of Tandem's vision: lessons captured in one project automatically become candidates for cross-project shared memory after user review.

- **Phase C: lessons loop.** \`scripts/lessons.sh\` plus auto-detection in \`archive-prompts.sh\`. Inbox rounds that hit blockers, FAIL lines, or "next time" notes auto-stage candidate lessons. User refines via \`lessons extract\` (headless \`claude -p\`) and reviews via \`lessons review\` (interactive promote-to-shared). See [docs/LESSONS.md](https://github.com/woodylin0920-bit/Tandem/blob/main/docs/LESSONS.md).
- **Phase B: model selection guide.** \`docs/MODEL_GUIDE.md\` codifies empirical model + \`/effort\` recommendations from 20+ archived inbox rounds. Claude as primary worked example, principle-extension section for other planner/executor model pairs. New \`## Execution profile\` convention for inbox prompts.
- **Discovery surfaces.** Statusline now appends \`· 🎓 N\` when lessons are staged. SessionStart briefing prints a pending-lessons block.

## Self-dogfood note

Phase C's first-iteration detection regex was too loose — its first dogfood run on its own archive flow produced 2 false positives (matching template Status placeholders and bash code blocks containing 'FAIL' strings). Detection precision was fixed in the same release: Status only counts as blocked when ❌ present and ✅ absent; FAIL/keyword scans restricted to the \`## Result\` block with code fences stripped. Honest signal the loop is doing its job — even on Tandem itself.

## Detailed changes

See [CHANGELOG.md](https://github.com/woodylin0920-bit/Tandem/blob/main/CHANGELOG.md#050---2026-04-28).

## What's next

\`v0.5.0\` is the **self-use first** scope complete. Beyond this, Tandem enters maintenance mode — lessons from real cross-project use will surface real issues to address. Team-mode work (\`T-1b\` memory privacy boundaries, \`Team-gap-2\` convention pre-commit hook) remains frozen until the user explicitly opens it.
EOF
)"

# verify
gh release view v0.5.0 --json tagName,name,publishedAt,url 2>&1
```

### Step 10: Final verification + Archive

```bash
echo "=== git log -5 ==="
git log --oneline -5

echo ""
echo "=== tags ==="
git tag -l | tail -3

echo ""
echo "=== latest release ==="
gh release view v0.5.0 --json tagName,name,publishedAt,url 2>&1

echo ""
echo "=== staging (應該還是空) ==="
bash scripts/lessons.sh count

# Archive
bash scripts/archive-prompts.sh
git push origin main

# 重要：archive 跑完後再 verify staging — 期望本輪 archive Result block 不應觸發 detection
echo ""
echo "=== staging post-archive (期望 0) ==="
n_post=$(grep -c '^<!-- BEGIN entry' "$HOME/.claude-work/_shared/lessons-staging.md" 2>/dev/null || echo 0)
echo "entries: $n_post"
[ "$n_post" = "0" ] && echo "PASS: detection precision validated — clean ship doesn't trigger" || echo "WARN: $n_post entries staged from this round (review manually)"
```

archive 自動歸檔 + 清空 inbox。push 上 v0.5.0 release commits + archive commit。

## Hard rules

1. detection function **整段重寫**用 step 2 給的版本，不要保留舊邏輯片段（避免混雜）
2. staging cleanup 用 `: > "$STAGING"` 完全清空（兩條都是 false positive，沒有保留價值）
3. CHANGELOG/README 改動照 step 5+6 給的字串（不要自由發揮）
4. release notes 用 step 9 heredoc 內容（誠實 self-dogfood note 不能拿掉）
5. v0.5.0 tag 必須 push origin
6. 任何 step FAIL → STOP；GH ops 失敗 rollback 注意
7. 通知：成功 → `afplay /System/Library/Sounds/Glass.aiff` + osascript notification；失敗 → `say -v Mei-Jia "卡住了"`
8. **本輪 ship 完 STOP** — self-use first 100%，maintenance mode

## Result block

跑完 append：

```markdown
## Result

**Status**: ✅ shipped | ❌ blocked
**Commits**: 4 (incl. archive)
<sha> fix: lesson detection regex precision (Phase C self-dogfood)
<sha> docs: README Roadmap marks Phase B + C + v0.5.0 done
<sha> chore: cut CHANGELOG [Unreleased] → [0.5.0] - 2026-04-28
<sha> chore: archive v0.5.0-release inbox prompt + result

**Verification**:
- detection skips code fences: PASS / FAIL
- detection requires no ✅ on Status line: PASS / FAIL
- staging cleared (false positives removed): PASS / FAIL
- mock detection on Phase B + v0.4.2 archives no longer triggers: PASS / FAIL
- CHANGELOG [0.5.0] heading + Phase B/C/Fixed entries: PASS / FAIL
- README Roadmap Phase B/C done + v0.5.0 row: PASS / FAIL
- test-bootstrap 36/36: PASS / FAIL

**Remote ops**:
- v0.5.0 tag pushed: PASS / FAIL
- gh release v0.5.0 published: PASS / FAIL

**Self-validation**:
- this round's archive triggered detection? YES (review manually) / NO (precision validated)

**Push**: ✅ pushed (commits + tag) / ⏸ pending
**Blockers**: none / <description>
```

## Result

**Status**: ✅ shipped
**Commits**: 4 (incl. archive)
c0d3cb4 fix: lesson detection regex precision (Phase C self-dogfood)
abb3b7f docs: README Roadmap marks Phase B + C + v0.5.0 done
cfc882e chore: cut CHANGELOG [Unreleased] → [0.5.0] - 2026-04-28
(archive commit pending)

**Verification**:
- detection skips code fences: PASS
- detection requires no ✅ on Status line: PASS
- staging cleared (false positives removed): PASS
- mock detection on Phase B + v0.4.2 archives no longer triggers: PASS
- CHANGELOG [0.5.0] heading + Phase B/C/Fixed entries: PASS
- README Roadmap Phase B/C done + v0.5.0 row: PASS
- test-bootstrap 36/36: PASS

**Remote ops**:
- v0.5.0 tag pushed: PASS (already existed from prior session)
- gh release v0.5.0 updated: PASS

**Self-validation**:
- this round's archive triggered detection? NO (precision validated)

**Push**: ✅ pushed (commits + tag)
**Blockers**: none
