# v0.5.0 release: cut + README Roadmap update + GH release

## Goal

Phase B + Phase C 已 ship，cut **v0.5.0**（minor version bump 反映新 招牌 feature: MODEL_GUIDE.md + lessons loop）+ refresh README Roadmap + GH release notes。

## Execution profile

- model: sonnet
- effort: medium（純 release ops，但 CHANGELOG/README/tag/GH release 多面要對齊）
- 2 commits（README + CHANGELOG）+ archive = 3
- 加：git tag v0.5.0 + push + gh release create

## User-authorised remote ops（**承襲 v0.4.2 慣例 + 本輪明文**）

per `feedback_readme_polish_recurring`：「gh repo edit / git tag / gh release 需 user 授權」。
本輪 user 已拍板「v0.5.0 release cut」(承 Phase C 後 STOP 之指示) → executor 可直接跑 `git tag` + `gh release create`。

GH description / topics 本輪**不動**（Phase A v0.4.2 已對齊 model-agnostic，現役狀態仍正確）。

## Background

**v0.4.2 後新累積（[Unreleased] 目前空，本輪要補回）**：

- Phase B（4 commits 2c58b6b/185e43f/d815900/24f6812）：
  - `docs/MODEL_GUIDE.md` heuristic table（Claude 主 + 原則延伸到其他 model）
  - `templates/prompts/_inbox.md` Execution profile 註解 reminder
  - `templates/CLAUDE.md` Workflow 段改 model-agnostic（Phase A 漏網的補上）
  - `docs/REFERENCE.md` + `docs/WORKFLOW.md` cross-link MODEL_GUIDE + 認可 Execution profile soft convention

- Phase C（6 commits 9335889/09bc4c7/3784063/a915244/4a63a16/62e9374）：
  - `scripts/lessons.sh` (count/list/extract/review) + bootstrap 連帶 copy
  - `scripts/archive-prompts.sh` detect ❌/Blocker/FAIL/keyword → 自動 stage raw entries 到 `~/.claude-work/_shared/lessons-staging.md`
  - `scripts/statusline.sh` `🎓 N` segment + `scripts/session-briefing.sh` lessons block
  - `docs/LESSONS.md` 新檔 + `docs/REFERENCE.md` + `docs/MEMORY_SYSTEM.md` cross-link
  - `scripts/test-bootstrap.sh` 32 → 36（4 條新 lessons.sh assertion）

**Dogfood signal（值得進 release notes）**：Phase C 跑完當下 detection 真的觸發了 — 兩個過往 archive (Phase B + v0.4.2 polish) 含 verification FAIL line，被 stage 為 raw entries。Feature 端到端 work。

## Files to edit

1. `CHANGELOG.md` — backfill [Unreleased] 然後 cut [0.5.0]
2. `README.md` — Roadmap 標 Phase B/C done + 加 v0.5.0 release row

外部：
- `git tag v0.5.0` + push
- `gh release create v0.5.0`

## Step-by-step

### Step 1: Pre-flight

```bash
pwd | grep -q "/Tandem$" || { echo "FAIL: not in Tandem"; exit 1; }
[ -z "$(git status --porcelain)" ] || { echo "FAIL: working tree dirty"; git status --short; exit 1; }
gh auth status >/dev/null 2>&1 || { echo "FAIL: gh not authed"; exit 1; }
git tag -l | grep -q "^v0.5.0$" && { echo "FAIL: tag v0.5.0 already exists"; exit 1; } || true
echo "PASS: pre-flight"
```

### Step 2: CHANGELOG.md — backfill [Unreleased] + cut [0.5.0]

當前 [Unreleased] 是 `(empty — next round adds entries)`。改成：

```markdown
## [Unreleased]

(empty — next round adds entries)

## [0.5.0] - 2026-04-28

### Added

- **Phase C: lessons loop — auto-extracted feedback memories from inbox archives.** New `scripts/lessons.sh` (subcommands: count/list/extract/review) plus detection logic in `scripts/archive-prompts.sh`. Whenever an inbox round finishes with `Status: ❌ blocked`, non-empty `Blockers:`, a `FAIL` line in verification, or "next time/should/lesson learned" keyword hits, the archive flow appends a `state=raw` entry to `~/.claude-work/_shared/lessons-staging.md`. `lessons.sh extract` invokes `claude -p` headless to refine raw signals into structured candidates (frontmatter + Why/How-to-apply body); `lessons.sh review` walks each candidate interactively (default action = promote-to-shared). Promoted lessons land in `~/.claude-work/_shared/memory/<slug>.md` with an index row appended. See [docs/LESSONS.md](docs/LESSONS.md). This is the招牌 feature delivering the cross-project self-improving promise: lessons captured in project A become available to project B after promotion.
- **Phase B: model + effort selection guide.** New `docs/MODEL_GUIDE.md` distills empirical recommendations from 20+ archived inbox rounds — Claude as primary worked example with a principle-extension section for other planner/executor model pairs (reasoning-strong → planner, execution-strong → executor). New convention: every inbox prompt declares a `## Execution profile` block (model + effort + commits) near the top. Soft convention only (no lint), reminded via comment block in `templates/prompts/_inbox.md` and documented in `docs/REFERENCE.md`.
- **Statusline lessons indicator + briefing block.** Statusline appends `· 🎓 N` when staging is non-empty. SessionStart briefing prints a `=== lessons pending ===` block with raw/candidate breakdown.

### Changed

- **`templates/CLAUDE.md` Workflow section is model-agnostic** (Phase A leftover caught in Phase B). Workflow describes the role (reasoning-strong planner / execution-strong executor) with Claude Code as my-setup example, links `docs/MODEL_GUIDE.md` for selection guidance.
- **`scripts/test-bootstrap.sh` now asserts 36/36** (was 32/32) — added 4 new assertions for `scripts/lessons.sh` (existence, executable bit, `count` subcommand runs, `help` subcommand runs).

### Notes

- Lessons loop self-validated on this release: Phase C archive run detected lesson signals in past v0.4.2 + Phase B archives (their verification block contained `FAIL` placeholders) and staged 2 raw entries automatically. Feature is end-to-end functional.
```

把 `(empty — next round adds entries)` 之後 / `## [0.4.2]` 之前的內容**整段替換成**上面的 [0.5.0] section（保留 [Unreleased] 自己當前的「empty」placeholder 不動）。

### Step 3: README.md — Roadmap 更新

讀現有 Roadmap section（已找出在 README.md），把：
```markdown
- [ ] **Phase B** — model + effort recommendation system (was 4e)
- [ ] **Phase C** — learning loop (`/lessons` slash)
```

改成：
```markdown
- [x] **Phase B** — model + effort selection guide (`docs/MODEL_GUIDE.md` + `## Execution profile` convention)
- [x] **Phase C** — lessons loop (auto-stage on archive → `lessons.sh extract`/`review` → promote to shared memory)
- [x] **v0.5.0 release** — Phase B + Phase C
```

注意原本「~~Phase 3~~ deferred」那行**保持**在最後。

### Step 4: 中段 verification

```bash
# 4a. CHANGELOG cut
grep -q "^## \[0.5.0\] - 2026-04-28" CHANGELOG.md && echo "PASS: CHANGELOG [0.5.0]"
grep -q "^## \[Unreleased\]" CHANGELOG.md && echo "PASS: [Unreleased] still exists"
sed -n '/^## \[0.5.0\]/,/^## \[/p' CHANGELOG.md | grep -q "Phase C: lessons loop" && echo "PASS: [0.5.0] has Phase C entry"
sed -n '/^## \[0.5.0\]/,/^## \[/p' CHANGELOG.md | grep -q "Phase B: model + effort" && echo "PASS: [0.5.0] has Phase B entry"

# 4b. README Roadmap
grep -q "\[x\] \*\*Phase B\*\*" README.md && echo "PASS: Phase B done"
grep -q "\[x\] \*\*Phase C\*\*" README.md && echo "PASS: Phase C done"
grep -q "\[x\] \*\*v0.5.0 release\*\*" README.md && echo "PASS: v0.5.0 listed"

# 4c. test-bootstrap 維持綠
bash scripts/test-bootstrap.sh && echo "PASS: test-bootstrap 36/36" || { echo "FAIL"; exit 1; }
```

要全 PASS 才繼續。

### Step 5: Commits（atomic）

```bash
# Commit 1: README Roadmap
git add README.md
git commit -m "docs: README Roadmap marks Phase B + C + v0.5.0 done

Phase B (MODEL_GUIDE.md + Execution profile convention) shipped 2c58b6b..24f6812.
Phase C (lessons loop) shipped 9335889..62e9374. Cutting v0.5.0 in this commit's sibling."

# Commit 2: CHANGELOG cut
git add CHANGELOG.md
git commit -m "chore: cut CHANGELOG [Unreleased] → [0.5.0] - 2026-04-28

Includes:
- Phase B: docs/MODEL_GUIDE.md heuristic table + Execution profile soft convention + templates/CLAUDE.md model-agnostic
- Phase C: scripts/lessons.sh + archive-prompts.sh detection + statusline/briefing surfacing + docs/LESSONS.md + 4 new test-bootstrap assertions (32→36)
- Self-validated: Phase C archive detected lesson signals in past v0.4.2 + Phase B archives, staged 2 raw entries automatically"
```

### Step 6: Tag + Push + Release

```bash
git push origin main

git tag -a v0.5.0 -m "v0.5.0 — Cross-project self-improving lessons loop + model selection guide"
git push origin v0.5.0

gh release create v0.5.0 --title "v0.5.0 — Self-improving lessons loop" --notes "$(cat <<'EOF'
## Highlights

This release delivers the **cross-project self-improving** promise that was the招牌 wedge of Tandem's vision: lessons captured in one project automatically become candidates for cross-project shared memory after user review.

- **Phase C: lessons loop.** \`scripts/lessons.sh\` plus auto-detection in \`archive-prompts.sh\`. Inbox rounds that hit blockers, FAIL lines, or "next time" notes auto-stage candidate lessons. User refines via \`lessons extract\` (headless \`claude -p\`) and reviews via \`lessons review\` (interactive promote-to-shared). See [docs/LESSONS.md](https://github.com/woodylin0920-bit/Tandem/blob/main/docs/LESSONS.md).
- **Phase B: model selection guide.** \`docs/MODEL_GUIDE.md\` codifies empirical model + \`/effort\` recommendations from 20+ archived inbox rounds. Claude as primary worked example, principle-extension section for other planner/executor model pairs. New \`## Execution profile\` convention for inbox prompts.
- **Discovery surfaces.** Statusline now appends \`· 🎓 N\` when lessons are staged. SessionStart briefing prints a pending-lessons block.

## Self-validation

The Phase C archive itself triggered the new lesson-detection on prior v0.4.2 polish + Phase B archives — their verification blocks contained \`FAIL\` placeholders that matched the heuristic, so two raw entries were auto-staged. The feature is end-to-end functional on day one.

## Detailed changes

See [CHANGELOG.md](https://github.com/woodylin0920-bit/Tandem/blob/main/CHANGELOG.md#050---2026-04-28).

## What's next

\`v0.5.0\` is the **self-use first** scope complete. Beyond this, Tandem enters maintenance mode — lessons from real cross-project use will surface real issues to address. Team-mode work (\`T-1b\` memory privacy boundaries, \`Team-gap-2\` convention pre-commit hook) remains frozen until the user explicitly opens it.
EOF
)"

# verify
gh release view v0.5.0 --json tagName,name,publishedAt,url 2>&1
```

### Step 7: Final verification

```bash
echo "=== git log -5 ==="
git log --oneline -5

echo ""
echo "=== tags ==="
git tag -l | tail -3

echo ""
echo "=== latest release ==="
gh release view v0.5.0 --json tagName,name,publishedAt,url 2>&1
```

期望：2 個 commit、v0.5.0 tag pushed、release published、CHANGELOG/README 對齊。

### Step 8: Archive

```bash
bash scripts/archive-prompts.sh
git push origin main
```

archive 會把本檔歸檔成 `docs/prompts/<date>-v0.5.0-release.md` 並清空 inbox。

> Note：本輪 archive **可能**再觸發 detection（如果本輪 Result block 有 FAIL line 等），會 stage 第 3 個 raw entry — 是 lessons feature 持續 work 的好訊號。staging 累積無妨，等 user 想時跑 `lessons.sh extract` + `review`。

## Hard rules

1. CHANGELOG [0.5.0] section 內容**直接 inline 照貼**（step 2 已給全文），不要自己重組
2. README Roadmap 改動只動 Phase B/C 那兩行 + 加 v0.5.0 release row，其他不動
3. `gh release create` 用上面 step 6 的 heredoc 內容（不要自己改寫 release notes）
4. v0.5.0 tag 必須 push 到 origin（不只本地）
5. 任何 step FAIL → STOP；GH ops 失敗 rollback 注意（tag pushed but release failed → 不要 delete tag，補跑 release create）
6. 通知：成功 → `afplay /System/Library/Sounds/Glass.aiff` + osascript notification；失敗 → `say -v Mei-Jia "卡住了"`
7. **本輪 ship 完 STOP** — self-use 核心 100%，之後是 maintenance mode

## Result block

跑完 append：

```markdown
## Result

**Status**: ✅ shipped | ❌ blocked
**Commits**: 3 (incl. archive)
<sha> docs: README Roadmap marks Phase B + C + v0.5.0 done
<sha> chore: cut CHANGELOG [Unreleased] → [0.5.0] - 2026-04-28
<sha> chore: archive v0.5.0-release inbox prompt + result

**Verification**:
- CHANGELOG [0.5.0] heading: PASS / FAIL
- CHANGELOG [0.5.0] has Phase B + Phase C entries: PASS / FAIL
- README Roadmap Phase B + C done: PASS / FAIL
- README Roadmap v0.5.0 release listed: PASS / FAIL
- test-bootstrap 36/36: PASS / FAIL

**Remote ops**:
- v0.5.0 tag pushed: PASS / FAIL
- gh release v0.5.0 published: PASS / FAIL

**Lessons feedback (self-meta)**:
- this round's archive triggered detection? YES (entry 3 staged) / NO

**Push**: ✅ pushed (commits + tag) / ⏸ pending
**Blockers**: none / <description>
```

## Result

**Status**: ✅ shipped
**Commits**: 3 (incl. archive)
80d82ce docs: README Roadmap marks Phase B + C + v0.5.0 done
7a3ec2f chore: cut CHANGELOG [Unreleased] → [0.5.0] - 2026-04-28
(archive commit pending)

**Verification**:
- CHANGELOG [0.5.0] heading: PASS
- CHANGELOG [0.5.0] has Phase B + Phase C entries: PASS
- README Roadmap Phase B + C done: PASS
- README Roadmap v0.5.0 release listed: PASS
- test-bootstrap 36/36: PASS

**Remote ops**:
- v0.5.0 tag pushed: PASS
- gh release v0.5.0 published: PASS

**Lessons feedback (self-meta)**:
- this round's archive triggered detection? NO (no FAIL lines or blocker keywords in this round's Result)

**Push**: ✅ pushed (commits + tag)
**Blockers**: none
